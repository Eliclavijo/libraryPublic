pipeline {
    agent any
    
    stages {
        stage('Clonar Repositorio') {
            steps {
                checkout scm
            }
        }
        stage('Ejecutar Pruebas') {
            steps {
                script {
                    // Ejecuta tus pruebas PHP aquí
                    sh 'phpunit'
                }
            }
        }
    }
}
